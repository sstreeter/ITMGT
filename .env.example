# ITMGT Configuration Sample
# Copy this file to .env and fill in your values
# Command: sudo cp .env.example .env

# [REQUIRED]
# The secret key used to authenticate requests from the client script.
# Clients must have this EXACT key in their config.json to work.
# Generate a secure key using: python3 -c "import secrets; print(secrets.token_hex(32))"
SBS_API_KEY=change-me-to-a-secure-random-string

# [OPTIONAL]
# Path to the SQLite database file.
# If using the 'sbsmgr' user (standard setup), ensure this path is writable by that user.
# Default: /srv/sbs_inventory/inventory.db
# SBS_DB_FILE=/srv/sbs_inventory/inventory.db
