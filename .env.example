# ITMGT Configuration Sample
# Copy this file to .env and fill in your values

# REQUIRED: The secret key used to authenticate requests from the client script.
# Generate a secure key using: python3 -c "import secrets; print(secrets.token_hex(32))"
SBS_API_KEY=change-me-to-a-secure-random-string

# OPTIONAL: Path to the SQLite database file.
# Defaults to /srv/sbs_inventory/inventory.db if not specified.
# SBS_DB_FILE=inventory.db
